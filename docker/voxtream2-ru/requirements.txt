# Dependencies imported by the text-to-WAV runtime.  Export and diagnostic
# tools intentionally have a separate, PyTorch-capable environment.
# requirements.lock records the complete dependency set used by the image.
numpy==1.26.4
cuda-python==12.6.0
onnxruntime==1.23.2
transformers==4.50.0
huggingface_hub==0.28.1
tokenizers==0.21.4
ruaccent==1.5.8.3
ru-normalizr==0.3.0
